[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

name = "example_project"
version = "0.1.0"
description = "A minimal example project for pytest"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.unittest]
testpaths = ["unittest_tests"]